message-broker:
  endpoint:
    name: rabbitmq     #Currently support rabbitmq only
  username: rabbitmq         #rabbitmq username to connect
  password: Networks123      #rabbitmq password to connect
  host: rabbitmq-service     #rabbitmq endpoint to connect
  port: 5672                 #rabbitmq port to connect
ssd:
  name: preview-saas-ssd
  enable: false
spinnaker:
  name: CDName       #Should be same as account name given in spinnaker setup
  extensibility:
    plugins:
      Opsmx.EchoEventPlugin:
        enabled: true
        version: 1.0.1
        config: null

microsoftteams:
  enabled: true

#slack:
#  app:
#    token: "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
#    signing_secret: "xxxxxxxxxxxxxxxxxxxxxxxxxxxx"
#
